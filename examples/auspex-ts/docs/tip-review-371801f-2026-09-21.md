# Tip review of `371801f` (post PR #51) — 2026-09-21

**Auditor:** Grok (Cursor cloud agent). Report only — no product code.  
**Tip:** `371801fa455db2b3e3d24d8a5ba289e83151c301` (`docs: loud claimOkProfile reuse gate + Save-is-not-fold next (#51)`).  
**Prior memos (attached this pass):** competitor-ahead-native, eng-field-opportunities, assessor-field-rescan — all written against `731ecac` (#50).  
**Constraint:** Auspex-NATIVE residuals only. No outbound. No secrets / `.auspex`. Do not merge as a fix.

Schema v1 required keys unchanged. Fail-closed not weakened. `ok` ≠ `claimOk` ≠ `claimOkProfile` holds. We still do **not** claim Alice-vs-Bob.

---

## Verdict

**ship-with-nits. No P0 contract lie.** #51 sealed the prior-memo P0s (loud `claimOkProfile` reuse gate, Save-is-not-fold `next` on the `--save-editor` miss path, triad-first Pages skim, measured-vs-demo labels, phone ≠ takeover card, Alice≠Bob on first screens).

What remains is **agent-copyable fence drift** and one **Save≠fold soft-success**: the fences agents actually paste still omit `--save-editor`, `phone.html` clipboard is weaker than the login packet, and a non-weak `await-login` `next` still says `Run auspex check` (skip finalize) when leftover folded sessionStorage looks counted.

Those are small, high-value native ships before 30 Sep. Do not open a cleanup PR from this file unless a later change already touches the same strings.

---

## Inventory: `731ecac` memos vs tip `371801f`

| Prior item | Memo | On `371801f` |
|---|---|---|
| Triad-first Pages skim + measured vs demo | competitor-ahead #1 / #4 | **Shipped** — `docs/index.html` header is triad + Alice≠Bob + Measured / Redacted demo / Recipe only **above** the replay iframe. `agents-sync` pins it. |
| Phone door card (not Handraise takeover) | competitor-ahead #2; assessor P1 | **Shipped** — Pages `.card`, README For Reviewers, AGENTS first-calls comment, `PHONE_HANDOFF_NOT_TAKEOVER`. |
| Loud `claimOkProfile` reuse gate in MCP / `next` / AGENTS | eng P0; assessor P0 | **Shipped** — `claimOkProfileReuseNext`, MCP descriptions, USAGE, cursor rule, `agent-receipt` tests. |
| Save≠fold / finalize-NOW `next` | eng P1; assessor P1 | **Shipped on the miss path** (`overlaySaveEditorNext`, `SAVE_NOT_FOLD_NOW`). **Residual** when `--save-editor` is omitted or leftover SS counts as non-weak. |
| Alice≠Bob non-claim on first screen | assessor P2; competitor-ahead | **Shipped** — Pages meta + header, README, PITCH, AGENTS first 20 lines. |
| MCP chain: phone Save paste as first-class | competitor-ahead #5 | **Partial** — `handoff.savePaste` + MCP `saveEditor` field exist. Clipboard on `phone.html` drifted. `login --wait` still omits `saveEditor`. |
| Dual-receipt CH + OneDrive pack | competitor-ahead #3 | **Still recipe-only** — `RECEIPTS.md` / CH demo JSON honest; no committed OneDrive PNG/receipt (PII). Leave unless a fail-closed redaction exists. |
| VWP settle / re-goto parity | eng P1 | **Shipped enough** — `#50` re-goto; VWP now polls expect (`PROFILE_CLAIM_SETTLE_MS` is a cap, not a mandatory sleep). Live check has `waitForURL(persistable)`. |
| `looksLikeAppProfile` kebab | PLAN E1; deferred-check #1 | **Gone from src** — `isConsistencyHubTarget` + public-marketing exclude. PLAN / deferred-check memos are stale. |
| Device / QR / origin allowlist tests | PLAN E4 / E5 | **Shipped** — `device-emulation.test.ts`, `qr-gen.test.ts`, `assert-receipt-origin.test.ts`. |
| Human X / Discord packet | competitor-ahead P1 | **Human** — `docs/showcase/DISCORD.md` still pre-#51 (no phone card, no reuse-gate sentence). Agent does not post. |

---

## Surfaces walked this pass

`examples/auspex-ts/src/**`, root + package `AGENTS.md`, root + package `README.md`, `PITCH.md`, `RECEIPTS.md`, `PLAN.md`, `docs/index.html`, `docs/phone.html`, `docs/showcase/DISCORD.md`, `src/tool-schema.ts`, `src/mcp-tools.ts`, `src/contract.ts`, `src/cli.ts` USAGE, `tests/**` (especially `agents-sync`, `login`, `editor-fold`, `agent-receipt`, `mcp-schema`), root + package `SECURITY.md`. Did **not** run live Solari. Did **not** fetch Pages / npm.

---

## Ranked residuals

### P1 — Copy-pasteable first-calls omit `--save-editor` — **S**

| | |
|---|---|
| **Why it still matters** | Phone Save is clipboard-only. Cookies persist when the agent POSTs `editor/save` (`--save-editor`) or the human Saves in the Solari console. Agents copy the fence, not the prose. |
| **Evidence** | Root + package `AGENTS.md` First calls: comment says “Then await-login --save-editor”; the commands are `await-login --profile myapp` and `await-login --profile consistencyhub` (no flag). `docs/index.html` Try it is `login` → `finalize-login` (skips await entirely). `examples/auspex-ts/DEMO.md` login recipe same omit. `needsHumanNext()` (`src/check.ts` ~130–135) says “await-login then finalize-login” with no `saveEditor`. |
| **Ship shape** | One-line fence fix + `agents-sync` needle that the **command** contains `--save-editor` / `saveEditor`. Pages Try it: `login` → `await-login --save-editor` → `finalize-login`. Do not invent a CDP keyboard bridge. |

### P1 — `phone.html` clipboard ≠ `phoneSavePaste` — **S**

| | |
|---|---|
| **Why** | The human pastes the phone line into chat. #51 loudened `src/profiles.ts` `phoneSavePaste` (`finalize-login NOW`, `editorSave` 401 / `editorFold` no-cdp, `claimOkProfile will not pass`). The live page still copies the weaker pre-#51 sentence (`if next says stale/weakSeed remint or finalize-now`). |
| **Evidence** | `docs/phone.html` `savePasteLine` (~211–215) vs `phoneSavePaste` (`src/profiles.ts` ~109–114). `tests/login.test.ts` pins `phoneSavePaste` and that `phone.html` contains `save-editor` / fold words — **not** string equality. Unused hash keys `h` / `p` / `t` are minted (`phoneHandoffUrl`) and never read by the page. |
| **Ship shape** | One shared paste string (or a test that `phone.html` embeds the same finalize-NOW / no-cdp / `claimOkProfile` needles). Optional: stop putting unused `h`/`p`/`t` in the hash (smaller leak surface). Still clipboard-only Save — do not POST from the phone. |

### P1 — Non-weak `await-login` `next` still says `Run auspex check` — **S**

| | |
|---|---|
| **Why** | #51 overlay strips that sentence **only** when `--save-editor` ran and the fold missed. First-calls omit the flag. Leftover folded SS that is counted and not inside the ~5m stale window is treated as success. |
| **Evidence** | `awaitNext` completed branch (`src/profile-persist.ts` ~355–366). `overlaySaveEditorNext` (~213) regex-strips `Run auspex check with --profile`. Production `waitForProfileSave` forwards origin **only** when the name is `consistencyhub` (~397–406); other profiles inspect with `origin === undefined` → `sessionStorage` undefined → not `weakSeed` → “Run auspex check”. `editor-fold.test.ts` still treats the check sentence as the “healthy” baseline. |
| **Ship shape** | Completed `next` always points at `finalize-login` unless `editorFold.ok` (today that is no-cdp). Optional: forward login `--url` origin for inspect, or document that non-CH inspect cannot count SS. Do not count SS without an origin. Do not build a refresh-token / CDP fold. |

### P1 — `login --wait` / MCP `wait: true` never passes `saveEditor` — **S**

| | |
|---|---|
| **Why** | Same contract hole on both doors. After phone Save, `--wait` polls cookies/origins only. If nothing else Saved, it times out; if leftover cookies exist, it returns `completed` without `editor/save`. Agents then skip `auspex_await_login`. |
| **Evidence** | `src/cli.ts` ~461–463 and `src/mcp-tools.ts` ~150: `liveAwaitLogin(profile, { sinceVersion })` — no `saveEditor`. `AUSPEX_CONTRACT` lists `wait` on login and `saveEditor` only on await-login (parity of fields is fine; the **composed** path is the lie). |
| **Ship shape** | Docs/`next`: `--wait` is console-Save wait, not the phone `--save-editor` path. Or pass `saveEditor: true` from `--wait` after a phone IME mint. Do not clone Handraise same-session resume. |

### P1 — Package `README` Run fence teaches a forbidden order — **S**

| | |
|---|---|
| **Why** | Package README is what a host sees if they only open `examples/auspex-ts`. The Run fence is not the recipe. |
| **Evidence** | `examples/auspex-ts/README.md` ~28–42: `check --name consistencyhub` **before** `finalize-login`, then `check --name consistencyhub --verify-with-profile`, then `npx auspex verify`. AGENTS: do not call `auspex_verify` after a default / VWP check. `agents-sync` does not pin this order. |
| **Ship shape** | Replace the dump with the same four-beat recipe as root README (login → await `--save-editor` → finalize → check; optional VWP). Drop the trailing `verify`. |

### P2 — Skim / outbound leftovers after #51 seals — **S**

| | |
|---|---|
| **Why** | Pages header is triad-first; the **visual** hero is still the Microsoft-wall iframe. Discord packet was not updated in #51. |
| **Evidence** | `docs/index.html` `<main>` opens with `iframe.watch` (`demo/replay.html`). `docs/showcase/DISCORD.md` has Pages + public `npx auspex-solari` + triad in the replay sentence, but no phone door, no “`ok` is not the reuse gate,” no Alice≠Bob. Human posts only. |
| **Ship shape** | Optional one Discord sentence (Aron). Optional caption under the iframe repeating “wall tape, not logged-in.” Do not record a logged-in session. Do not chase ARC GIFs. |

### P2 — Dead / stale memos after shipped eng — **S**

| File | Stale claim | Tip truth |
|---|---|---|
| `docs/vwp-magic-sleeps-2026-09-19.md` | VWP “magic sleep 2s / 3s” | `sandbox.ts` polls expect; `receipt.test.ts` forbids `abortableSleep(PROFILE_CLAIM_SETTLE_MS`. |
| `docs/deferred-check-2026-09-19.md` | `looksLikeAppProfile` live at cited lines | Symbol gone; `isWeakSeed` + `isConsistencyHubTarget`. |
| `PLAN.md` E1–E5 inventory | kebab / device tests / origin tautology still open | E1/E4/E5 done; PLAN banner already says historical. |
| Root `SECURITY.md` | Package SECURITY includes “responsible disclosure” | Package file ends at demo receipts — no disclosure section. |

**Ship shape:** one-line banners (“superseded on `371801f`”) or delete the rotting PLAN IDs. Do not execute PLAN again.

### P2 — Leftover `isAuthGatedAnonymousVerifyHost` — **S**

`shouldVerifyCheck` already skips any attached profile on a non-public-marketing URL (`src/fail-closed.ts`). The two-host helper + `AUTH_GATED_ANONYMOUS_VERIFY_HOSTS` remain for tests / dist grep. Harmless. Delete only if already touching the file.

### P2 — Inspect-without-URL strips sessionStorage — **S** (leave)

`profile-status.ts` ~135–137 drops `sessionStorage` when `!url`. Production CH status has a saved URL. Do not “fix” by counting SS without origin.

### P2 — OneDrive dual receipt — **S–M** (leave unless redaction is fail-closed)

Prior memo P1. Tip is honest: recipe only, no PNG. Shipping a redacted JSON without a live 2026-09-21 run would be theater. Prefer refuse > blurry lie.

---

## Honesty / agent-ops (what is **not** a lie)

- Receipt `ok` does **not** fold `claimOkProfile` (`agentReceiptOk` + `claimOkProfileReuseNext`).
- `--verify` ≠ `--verify-with-profile` on CLI, MCP, USAGE, AGENTS.
- VWP timeout with `anonymousClaimSkipped` stays `reason: matched` / `ok` from integrity; overlay `network` when integrity fails (retry-shaped, documented).
- Phone Save does not `location.href` to Solari (401). Clipboard + agent `--save-editor` is the door.
- CH demo receipt triad is honest (`ok=true`, `claimOk=false` + skip, `claimOkProfile=true`); `sessionStorage` omitted with `demoNote`.
- `npx auspex-solari` vs npm `auspex` warning present.

---

## MCP ↔ CLI parity

Field tables match (`AUSPEX_CONTRACT`). `saveEditor` is listed. Descriptions name the reuse gate and Save≠fold.

**Not a schema miss — a composed-path miss:** `login --wait` / `wait: true` ≠ `await-login --save-editor`. Phone clipboard ≠ `handoff.savePaste`.

---

## Tests / security nits

- `agents-sync` is strong on #51 prose (reuse gate, seed door, Pages header) and weak on **command fences** (`--save-editor` on the typed line, package Run order, `phone.html` === `phoneSavePaste`).
- Fence-token sanitize, password fill dual-check, desktop `--type` refuse, record+profile/CH/SSO gates, extract-zip override — still hold on read.
- Phone JWT `exp` is decoded without signature check (display-only; unknown exp locks the UI). Unused `h`/`p`/`t` in the hash are extra secret surface if the URL is forwarded.
- Root SECURITY promises a disclosure section that the package file does not have.

---

## REFUSE (explicit)

| Temptation | Why refuse |
|---|---|
| Handraise same-session live takeover / QR resume of the check Chrome | Contradicts phone seed/handoff + finalize. Overlay / noVNC on a phone will not open the keyboard. |
| ARC reflex / cost / mock-metrics theater | Wrong product. Honesty brand. |
| Fare Board / Fyt / consumer clone | Founder refuse. |
| Identity Guard Alice-vs-Bob detector | Deliberate non-claim. A seeded profile can still be the wrong Microsoft user. |
| CDP keyboard bridge / editor fold invent | `editor-fold-refresh-nogo-2026-09-20.md`: no-cdp today; 409 while editor open. |
| ConsistencyHub `refreshToken` silent restore | Same nogo memo; can surface password/OTP. |
| FlakeLab flake scientist / agentbench leaderboard / Gauntlet arena | Wrong object. |
| Logged-in `--record` / `--allow-record-profile` for consistencyhub | Existing refuse. |
| Fold `claimOkProfile` into `ok` | Thesis. |
| npm download vanity / intern-ping | Human distribution. |
| Fourth primitive | Check / verify / desktop only. |

---

## Priority before 30 Sep (native only)

| Pri | Item | Size | Code? |
|---|---|---|---|
| **P1** | First-calls + Pages Try it + DEMO + `needsHumanNext`: typed `--save-editor` | S | Docs + one string |
| **P1** | `phone.html` paste === `phoneSavePaste` (+ pin test) | S | `docs/phone.html` |
| **P1** | Completed await `next`: finalize unless `editorFold.ok` | S | `profile-persist.ts` |
| **P1** | `login --wait` honesty or pass `saveEditor` after phone mint | S | CLI + MCP |
| **P1** | Package README Run fence = recipe, no trailing `verify` | S | Docs |
| **P2** | Discord one-liner (human) + iframe caption | S | Copy |
| **P2** | Banner stale vwp / deferred / SECURITY pointer | S | Docs |
| **Defer** | OneDrive redacted artifact, delete `isAuthGated*`, inspect-without-URL | — | Leave |

**No product fixes in this PR. Do not merge as a fix.**
