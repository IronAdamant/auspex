{
  "mcpServers": {
    "auspex": {
      "_comment": "After clone: npm install && npm run build:mcp (dist/ is gitignored). Then restart Claude. npx auspex-mcp fail-closes DistMissing if dist/mcp.mjs is absent.",
      "command": "npx",
      "args": ["auspex-mcp"],
      "cwd": "/path/to/auspex"
    }
  }
}
