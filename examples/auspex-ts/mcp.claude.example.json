{
  "mcpServers": {
    "auspex": {
      "_comment": "Published package. No clone. Put SOLARI_API_KEY in env. Clone path after npm run build:mcp is npx auspex-mcp with cwd set to the repo. Missing dist/mcp.mjs fail-closes DistMissing.",
      "command": "npx",
      "args": ["-p", "auspex-solari", "auspex-mcp"],
      "env": {
        "SOLARI_API_KEY": "slr_live_…"
      }
    }
  }
}
